<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <permission
        android:name="com.google.android.markup.remix.permission.SAVE_STICKER"
        android:protectionLevel="signature" />
    <uses-permission android:name="com.google.android.markup.remix.permission.SAVE_STICKER" />

    <queries>
        <package android:name="com.google.android.apps.pixel.creativeassistant" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/markup"
        android:supportsRtl="true"
        android:theme="@style/MarkupTheme">

        <activity
            android:name=".AnnotateActivity"
            android:exported="true"
            android:colorMode="wideColorGamut"
            android:theme="@style/MarkupTheme">
            <intent-filter>
                <action android:name="android.intent.action.EDIT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <activity
            android:name=".sticker.StickerActivity"
            android:exported="true"
            android:theme="@style/MarkupTheme"
            android:windowSoftInputMode="adjustResize" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.google.android.markup"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
